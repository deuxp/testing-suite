/*
Purpose:  multi pointers; checks for average of pairs thet match target
inputs:   Array, int -> 
output:   boolean -> result of comparison
*/
const averagePair = (props = [], target) => {
  if (props.length <= 1) return false;

  let left = 0; // calculate middle
  let right = 1; // calculate middle

  // start in the middle
  // take the avg
  // if the avg is higher than target
  // increment the left head lower
  // if the avg is lower than target
  // increment the right head higher

  return null;
};

module.exports = { averagePair };
